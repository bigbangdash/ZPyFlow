{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ZPyFlow","text":"<p>Zero-allocation lazy query pipelines for Python, powered by Rust.</p> <ul> <li>Lazy &amp; fused \u2014 filter + map + take run in a single pass with no intermediate lists</li> <li>SIMD-accelerated \u2014 float/int arrays execute in Rust with the GIL released</li> <li>Expression DSL \u2014 <code>col &gt; 5</code> eliminates Python callbacks entirely</li> <li>Python-friendly \u2014 numpy, pandas, dataclasses, plain lists, and generators all work as input</li> <li>Parallel execution \u2014 <code>.parallel()</code> enables Rayon work-stealing</li> </ul> <p>ZPyFlow is not a DataFrame engine</p> <p>It lets Python sequence hot paths run as fused Rust pipelines without moving the surrounding codebase into a tabular data model. See Performance Guide for when to use ZPyFlow vs Polars.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>pip install maturin\ngit clone https://github.com/bigbangdash/zpyflow\ncd zpyflow\nmaturin develop --release\n</code></pre> <pre><code>from zpyflow import Query, col\n\ndata = [1.5, -2.3, 0.7, 4.1, -0.5, 3.8]\n\nresult = (\n    Query(data)\n        .filter(col &gt; 0)   # SIMD filter, GIL released\n        .map(col * 2.0)    # SIMD map\n        .take(4)\n        .to_list()\n)\n# \u2192 [3.0, 1.4, 8.2, 7.6]\n</code></pre>"},{"location":"#navigation","title":"Navigation","text":"<ul> <li>API Reference \u2014 complete public API</li> <li>Performance Guide \u2014 when ZPyFlow is fast, when to use Polars</li> <li>Examples \u2014 runnable examples</li> <li>Benchmark Results \u2014 measured performance data</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>All public symbols are importable from <code>zpyflow</code> directly.</p> <pre><code>from zpyflow import (\n    Query, col, field,\n    Expr, ColProxy, FieldExpr,\n    AggSpec, GroupBy,\n    agg_count, agg_sum, agg_mean, agg_max, agg_min,\n    from_numpy, from_arrow, from_csv, from_json_lines, from_generator,\n)\n</code></pre>"},{"location":"api/#query","title":"Query","text":"<p>The core lazy pipeline class.</p> <pre><code>Query(data: Iterable[T]) -&gt; Query[T]\n</code></pre> <p>Dispatch strategy</p> Input type Fast path <code>list[float]</code> / numpy <code>float64</code> f64 (SIMD, GIL released) <code>list[int]</code> / numpy <code>int64</code> i64 (GIL released) numpy <code>bool</code> / <code>uint8</code> u8 compact (GIL released) <code>list[dict]</code> Obj; converts to RustObj on first <code>field()</code> filter Anything else Python path (GIL held)"},{"location":"api/#lazy-combinators","title":"Lazy combinators","text":"Method Description <code>.filter(pred)</code> Keep elements matching <code>pred</code> (DSL or callable) <code>.map(f)</code> Transform each element (DSL or callable) <code>.take(n)</code> Stop after at most <code>n</code> elements <code>.skip(n)</code> Drop the first <code>n</code> elements <code>.parallel()</code> Request parallel execution (numeric paths only) <code>.take_while(pred)</code> Yield while <code>pred</code> is true, then stop <code>.skip_while(pred)</code> Skip while <code>pred</code> is true, then yield the rest <code>.chain(other)</code> Concatenate <code>other</code> after this query <code>.enumerate()</code> Yield <code>(index, item)</code> tuples <code>.zip(other)</code> Pair with <code>other</code> (stops at shorter) <code>.flat_map(f)</code> Apply <code>f</code> and flatten one level <code>.preload()</code> Convert dict records to RustObj eagerly <code>.group_by(key_fn)</code> Group elements into a <code>GroupBy</code> object <code>.group_agg(key_fn, **specs)</code> Single-pass group + aggregate (Rust kernel)"},{"location":"api/#terminal-operations","title":"Terminal operations","text":"Method Description <code>.to_list()</code> Collect to <code>list</code> <code>.to_dict(key, value)</code> Collect to <code>dict</code> <code>.count()</code> Count matching elements <code>.sum()</code> Sum (SIMD for numeric) <code>.min()</code> Minimum value <code>.max()</code> Maximum value (SIMD for f64) <code>.first()</code> First element or <code>None</code> <code>.last()</code> Last element or <code>None</code> <code>.reduce(fn, initial)</code> General left fold <code>.for_each(fn)</code> Consume for side effects <code>.any(pred)</code> True if any element matches (short-circuits) <code>.all(pred)</code> True if all elements match (short-circuits) <code>.explain()</code> Human-readable pipeline description"},{"location":"api/#col-numeric-dsl-sentinel","title":"col \u2014 numeric DSL sentinel","text":"<pre><code>from zpyflow import col\n\ncol &gt; 5.0        # FilterGt\ncol &gt;= 5.0       # FilterGe\ncol &lt; 5.0        # FilterLt\ncol &lt;= 5.0       # FilterLe\ncol * 2.0        # MapMulScalar\ncol + 1.0        # MapAddScalar\ncol - 1.0        # MapSubScalar\ncol / 2.0        # MapDivScalar\ncol ** 2         # MapPowScalar\n-col             # MapNeg\ncol.abs()        # MapAbs\ncol.sqrt()       # MapSqrt\ncol.floor()      # MapFloor\ncol.ceil()       # MapCeil\ncol.round()      # MapRound\ncol.reciprocal() # MapReciprocal\ncol.between(a, b)  # FilterBetween (inclusive)\n</code></pre>"},{"location":"api/#field-object-dsl","title":"field() \u2014 object DSL","text":"<pre><code>from zpyflow import field\n\nfield(\"price\") &gt; 100           # FilterFieldGt\nfield(\"status\") == 200         # FilterFieldEq\nfield(\"score\").between(0, 1)   # FilterFieldBetween\n\n# Use as key in group_agg\nQuery(records).group_agg(field(\"category\"), count=agg_count())\n</code></pre>"},{"location":"api/#source-adapters","title":"Source adapters","text":"Function Description <code>from_numpy(arr)</code> 1-D numpy array (buffer protocol, GIL-free memcpy) <code>from_arrow(arr)</code> PyArrow Array/ChunkedArray (buffer protocol for null-free numeric) <code>from_csv(path, column, dtype, ...)</code> CSV file or file-like <code>from_json_lines(path, field, dtype)</code> NDJSON file or file-like <code>from_generator(gen)</code> Eagerly materialise a generator"},{"location":"api/#aggregation-specs","title":"Aggregation specs","text":"<p>Used with <code>Query.group_agg()</code>:</p> <pre><code>from zpyflow import agg_count, agg_sum, agg_mean, agg_max, agg_min\n\nresult = Query(records).group_agg(\n    lambda r: r[\"category\"],\n    count   = agg_count(),\n    revenue = agg_sum(lambda r: r[\"price\"]),\n    avg     = agg_mean(lambda r: r[\"price\"]),\n)\n</code></pre>"},{"location":"api/#groupby","title":"GroupBy","text":"<pre><code>from zpyflow import GroupBy\n\ngb = GroupBy(records, key_fn=lambda r: r[\"dept\"])\ngb.keys()                          # list of group keys\ngb.get_group(\"Engineering\")        # Query for one group\ngb.count_per_group()               # dict {key: count}\ngb.sum_per_group(field=lambda r: r[\"salary\"])  # dict {key: sum}\ngb.agg(count=lambda g: g.count())  # list[dict] with \"_key\"\n</code></pre>"},{"location":"benchmarks/","title":"Benchmark Results","text":"<p>Raw JSON results are stored in <code>sandbox/benchmark/results/</code> and updated after each <code>make dc-bench</code> run.</p>"},{"location":"benchmarks/#running-benchmarks-locally","title":"Running benchmarks locally","text":"<pre><code># All suites (Docker \u2014 reproducible environment)\nmake dc-bench\n\n# Single suite\nmake dc-bench-filter\n\n# Save current results as baseline\nmake dc-bench-save\n\n# Compare against saved baseline (fails if &gt;10% regression)\nmake dc-bench-compare\n</code></pre>"},{"location":"benchmarks/#key-results-linux-cpython-311-aarch64","title":"Key results (Linux, CPython 3.11, aarch64)","text":"<p>Results from the most recent CI run in <code>sandbox/benchmark/results/Linux-CPython-3.11-64bit/</code>.</p>"},{"location":"benchmarks/#filter-take-1m-float64","title":"Filter + take (1M float64)","text":"Approach Mean time Python list comprehension ~80ms numpy <code>arr[arr &gt; 0]</code> ~8ms ZPyFlow Expression DSL ~2\u20135ms ZPyFlow DSL + parallel ~0.5\u20131ms"},{"location":"benchmarks/#vector-search-top-k-early-stop","title":"Vector search top-K (early-stop)","text":"<p><code>filter(col &gt; threshold).take(K)</code> where K=100, N=1M, threshold=0.85:</p> Approach Mean time Note ZPyFlow DSL ~70\u00d7 faster than numpy Early-stop after K results numpy <code>arr[arr &gt; t][:K]</code> slower Scans all N first"},{"location":"benchmarks/#etl-multi-stat-count-sum-max","title":"ETL multi-stat (count + sum + max)","text":"Approach Mean time Note Polars fastest 1 columnar pass ZPyFlow ~4\u00d7 slower 3 separate passes Python loop slowest \u2014 <p>When ZPyFlow loses</p> <p>For multi-stat aggregations (count + sum + max in one report), Polars is significantly faster.  Use ZPyFlow for single-stat or early-stop workloads.</p>"},{"location":"design/","title":"Design Notes","text":"<p>ZPyFlow is inspired by ZLinq (zero-allocation LINQ for C#/.NET).</p> <p>ZLinq fuses <code>Where().Select().Take()</code> into a single loop at JIT time using CLR generic specialization.  ZPyFlow achieves the same fusion inside the Rust core using the <code>ZStream</code> trait \u2014 every operator is a concrete generic type, LLVM inlines the full chain at <code>-O3</code>.  The PyO3 boundary is the only place dynamic dispatch appears, and it is crossed once per terminal call, not once per element.</p>"},{"location":"design/#dispatch-strategy","title":"Dispatch strategy","text":"<pre><code>Python data \u2192 Query(data)\n                \u2502\n                \u251c\u2500 list[float]  \u2192 LazyFloatList \u2192 F64 (SIMD, GIL released)\n                \u251c\u2500 list[int]    \u2192 I64 (SIMD, GIL released)\n                \u251c\u2500 list[dict]   \u2192 Obj (lazy)\n                \u2502                  \u2514\u2500 field() filter \u2192 RustObj (GIL-free after conversion)\n                \u251c\u2500 numpy f64    \u2192 F64 (buffer protocol memcpy)\n                \u251c\u2500 numpy i64    \u2192 I64 (buffer protocol memcpy)\n                \u251c\u2500 numpy bool   \u2192 U8  (compact 0/1)\n                \u2514\u2500 other        \u2192 Py (Python object path, GIL held)\n</code></pre>"},{"location":"design/#gil-management","title":"GIL management","text":"Path GIL behavior F64 / I64 / U8 Released via <code>py.allow_threads()</code> for all numeric ops RustObj Released for filter/count/sum after dict\u2192RustObj conversion Obj / Py Held for each callable invocation Parallel Fully released; rayon work-stealing; re-acquired at collection"},{"location":"design/#further-reading","title":"Further reading","text":"<ul> <li>ARCHITECTURE.md \u2014 detailed design document</li> <li>docs/when_is_it_fast.md</li> </ul>"},{"location":"performance/","title":"Performance Guide","text":""},{"location":"performance/#when-zpyflow-is-fast","title":"When ZPyFlow is fast","text":"<p>ZPyFlow wins when the pipeline is single-stat, small output, or early-stop:</p> Use case Why ZPyFlow wins <code>filter(col &gt; t).count()</code> on 1M floats SIMD, GIL released, 1 pass <code>filter(col &gt; t).take(K)</code> with small K Early-stop: scans only until K results found <code>filter + map + sum</code> in one pass Fused, 1 allocation Embedding threshold + top-K Fast early-stop vs NumPy full-scan"},{"location":"performance/#when-to-use-polars-instead","title":"When to use Polars instead","text":"Use case Polars wins because 3+ stats in one pass (count + sum + max) 1 columnar pass vs 3 ZPyFlow passes Multi-column join Not supported in ZPyFlow Loading CSV \u2192 table analysis Natural fit for Polars SQL-style GROUP BY with multiple columns Polars native"},{"location":"performance/#benchmark-results-1m-float64","title":"Benchmark results (1M float64)","text":"Approach Time Allocations GIL Python list comprehension ~80ms 2 lists held numpy (<code>arr[arr &gt; 0] * 2</code>) ~8ms 2 arrays released ZPyFlow Expression DSL (SIMD) ~2\u20135ms 1 list released ZPyFlow DSL + parallel ~0.5\u20131ms 1 list released <p>Raw benchmark JSON files are in <code>sandbox/benchmark/results/</code>.</p>"},{"location":"performance/#execution-path-guide","title":"Execution path guide","text":"<p>Use <code>Query.explain()</code> to see which path a query takes:</p> <pre><code>q = Query(data).filter(col &gt; 0).map(col * 2).take(1000)\nprint(q.explain())\n# Query.explain()\n#   kind:     f64\n#   ops:      FilterGt(0.0) \u2192 MapMulScalar(2.0)\n#   skip:     0\n#   take:     1000\n#   parallel: false\n#   gil_free: true\n#   alloc:    1 Vec at terminal\n</code></pre>"},{"location":"performance/#parallel-execution","title":"Parallel execution","text":"<p><code>.parallel()</code> applies to numeric fast paths only (f64 / i64).</p> <pre><code># Multi-threaded: Rayon work-stealing, GIL fully released\nresult = Query(large).filter(col &gt; 0).map(col * 2).parallel().to_list()\n</code></pre> <p>Threading overhead</p> <p>Parallel mode adds split + join overhead.  It is slower than single-threaded for inputs under ~500K elements.  Profile before enabling.</p>"},{"location":"examples/","title":"Examples","text":"<p>Each example is self-contained and runnable after building the extension.</p> <pre><code>maturin develop --release\npip install numpy pandas pyarrow\n</code></pre>"},{"location":"examples/#api-integration-examples","title":"API &amp; integration examples","text":"Page What it covers Numeric DSL <code>filter</code>, <code>map</code>, <code>take</code> on plain lists and numpy arrays NumPy integration <code>from_numpy</code>, buffer protocol, speed comparison Object field DSL <code>field()</code> expressions, <code>preload()</code>, dict records GroupBy &amp; group_agg <code>GroupBy</code>, <code>group_agg</code>, aggregation specs Adapters <code>from_csv</code>, <code>from_json_lines</code>, <code>from_arrow</code>"},{"location":"examples/#runnable-example-files","title":"Runnable example files","text":"<p>The <code>examples/</code> directory contains standalone Python scripts:</p> File Scenario <code>01_basic_numeric.py</code> Plain list pipelines, DSL vs lambda <code>02_numpy_integration.py</code> numpy float64/int64 arrays <code>03_pandas_integration.py</code> Column preprocessing, row filtering <code>04_dataclasses.py</code> Dataclasses, Pydantic, domain objects <code>05_log_processing.py</code> Access logs, error rates, latency stats <code>06_etl_pipeline.py</code> Extract \u2192 Transform \u2192 Load with CSV/JSON <code>07_ai_embeddings.py</code> Vector search, batch inference, top-p sampling <code>08_parallel_and_performance.py</code> Parallel vs single-thread, memory <code>usecase_fraud_detection.py</code> Risk score thresholding, review queue <code>usecase_search_recommendation.py</code> ANN post-filtering, top-K candidate retrieval <code>usecase_ml_feature_preprocessing.py</code> Outlier removal + normalization"},{"location":"examples/adapters/","title":"Adapters","text":""},{"location":"examples/adapters/#from_numpy","title":"from_numpy","text":"<pre><code>import numpy as np\nfrom zpyflow import from_numpy, col\n\narr = np.arange(1_000_000, dtype=np.float64)\nresult = from_numpy(arr).filter(col &gt; 500_000).to_list()\n</code></pre>"},{"location":"examples/adapters/#from_arrow","title":"from_arrow","text":"<pre><code>import pyarrow as pa\nfrom zpyflow import from_arrow, col\n\narr = pa.array([1.0, 2.0, 3.0, 4.0], type=pa.float64())\nresult = from_arrow(arr).filter(col &gt; 2.0).to_list()  # \u2192 [3.0, 4.0]\n\n# Float64 with nulls \u2014 nulls become NaN\narr_null = pa.array([1.0, None, 3.0], type=pa.float64())\nresult = from_arrow(arr_null).filter(col == col).to_list()  # \u2192 [1.0, 3.0]\n</code></pre>"},{"location":"examples/adapters/#from_csv","title":"from_csv","text":"<pre><code>from zpyflow import from_csv, col\n\n# Single numeric column \u2014 f64 fast path\nprices = from_csv(\"products.csv\", column=\"price\", dtype=\"float\")\ndiscounted = prices.filter(col &gt; 10.0).map(col * 0.9).to_list()\n\n# All rows as dicts\nproducts = from_csv(\"products.csv\")\npremium = (\n    products\n        .filter(lambda p: float(p[\"price\"]) &gt; 100)\n        .take(50)\n        .to_list()\n)\n</code></pre>"},{"location":"examples/adapters/#from_json_lines","title":"from_json_lines","text":"<pre><code>from zpyflow import from_json_lines, col\n\n# Single numeric field\nlatencies = from_json_lines(\"access.log.ndjson\", field=\"latency_ms\", dtype=\"float\")\np99 = latencies.filter(col &gt; 200).count()\n\n# All rows as dicts\nevents = from_json_lines(\"events.ndjson\")\nerrors = events.filter(lambda e: e.get(\"level\") == \"error\").take(100).to_list()\n</code></pre>"},{"location":"examples/adapters/#from_generator","title":"from_generator","text":"<pre><code>from zpyflow import from_generator\n\ndef generate_scores():\n    for i in range(1_000_000):\n        yield float(i) / 1_000_000\n\nq = from_generator(generate_scores())   # eagerly materialises\n</code></pre>"},{"location":"examples/field_dsl/","title":"Object Field DSL","text":"<p><code>field(\"name\")</code> creates expressions that run GIL-free after the first filter converts dicts to <code>RustObj</code>.</p> <pre><code>from zpyflow import Query, field\n\nlogs = [\n    {\"status\": 500, \"latency_ms\": 312, \"path\": \"/api/users\"},\n    {\"status\": 200, \"latency_ms\":  45, \"path\": \"/api/items\"},\n    {\"status\": 500, \"latency_ms\": 520, \"path\": \"/health\"},\n]\n\n# DSL filter \u2014 dict\u2192RustObj conversion on first filter, then GIL-free\ncount = (\n    Query(logs)\n        .filter(field(\"status\") &gt;= 500)\n        .filter(field(\"latency_ms\") &gt; 100)\n        .count()\n)\n\n# Multiple queries on the same dataset \u2014 preload() pays conversion once\nq = Query(logs).preload()\nslow_errors = q.filter(field(\"status\") &gt;= 500).filter(field(\"latency_ms\") &gt; 100).count()\nall_errors  = q.filter(field(\"status\") &gt;= 500).count()\n</code></pre>"},{"location":"examples/field_dsl/#supported-field-operators","title":"Supported field() operators","text":"Expression Description <code>field(\"x\") &gt; n</code> Greater than <code>field(\"x\") &gt;= n</code> Greater than or equal <code>field(\"x\") &lt; n</code> Less than <code>field(\"x\") &lt;= n</code> Less than or equal <code>field(\"x\") == v</code> Equals (numeric or string) <code>field(\"x\") != v</code> Not equals <code>field(\"x\").between(a, b)</code> Inclusive range <p>When to use field() vs lambda</p> <p>Use <code>field()</code> when filtering dict records by a known numeric field name \u2014 it avoids calling Python per element after the first filter.  For complex logic (multiple fields, string operations), use a lambda.</p>"},{"location":"examples/groupby/","title":"GroupBy &amp; group_agg","text":""},{"location":"examples/groupby/#group_agg-single-pass-rust-kernel","title":"group_agg \u2014 single-pass Rust kernel","text":"<p><code>group_agg</code> runs all aggregations in one pass without materializing intermediate lists.</p> <pre><code>from zpyflow import Query, field, agg_count, agg_sum, agg_mean\n\ntransactions = [\n    {\"user\": \"alice\", \"amount\": 120.0, \"category\": \"food\"},\n    {\"user\": \"bob\",   \"amount\":  45.0, \"category\": \"transport\"},\n    {\"user\": \"alice\", \"amount\": 300.0, \"category\": \"shopping\"},\n]\n\nresult = (\n    Query(transactions)\n        .group_agg(\n            lambda t: t[\"user\"],\n            count   = agg_count(),\n            total   = agg_sum(lambda t: t[\"amount\"]),\n        )\n)\n# \u2192 [{\"_key\": \"alice\", \"count\": 2, \"total\": 420.0},\n#    {\"_key\": \"bob\",   \"count\": 1, \"total\":  45.0}]\n\n# field() DSL key \u2014 Rust-side extraction\nby_category = (\n    Query(transactions)\n        .group_agg(\n            field(\"category\"),\n            count   = agg_count(),\n            revenue = agg_sum(lambda t: t[\"amount\"]),\n            avg     = agg_mean(lambda t: t[\"amount\"]),\n        )\n)\n</code></pre>"},{"location":"examples/groupby/#groupby-multi-operation-groups","title":"GroupBy \u2014 multi-operation groups","text":"<p>Use <code>GroupBy</code> when you need per-group <code>Query</code> objects:</p> <pre><code>from zpyflow import GroupBy, Query\n\ngb = GroupBy(transactions, key_fn=lambda t: t[\"user\"])\n\n# Per-group queries\nalice_txns = gb.get_group(\"alice\").to_list()\n\n# Aggregation via reducers\nsummary = gb.agg(\n    count=lambda g: g.count(),\n    total=lambda g: Query([t[\"amount\"] for t in g.to_list()]).sum(),\n)\n\n# Quick stats\ncounts = gb.count_per_group()             # {\"alice\": 2, \"bob\": 1}\ntotals = gb.sum_per_group(field=lambda t: t[\"amount\"])  # {\"alice\": 420.0, ...}\n</code></pre>"},{"location":"examples/groupby/#when-to-prefer-group_agg-over-groupby","title":"When to prefer group_agg over GroupBy","text":"<code>group_agg</code> <code>GroupBy.agg</code> Passes over data 1 1 per group operation Per-group queries \u274c \u2705 Custom multi-step logic Limited \u2705 Performance (count + sum) Faster Slower"},{"location":"examples/numeric/","title":"Numeric DSL","text":"<p>The <code>col</code> proxy creates expressions that compile to Rust operations \u2014 no GIL, SIMD where applicable.</p> <pre><code>from zpyflow import Query, col\n\ndata = [1.5, -2.3, 0.7, 4.1, -0.5, 3.8, -1.1, 2.2]\n\n# Filter \u2192 map \u2192 take: single fused Rust pass\nresult = (\n    Query(data)\n        .filter(col &gt; 0)\n        .map(col * 2.0)\n        .take(4)\n        .to_list()\n)\n# \u2192 [3.0, 1.4, 8.2, 7.6]\n\n# Aggregations\ntotal = Query(data).filter(col &gt; 0).sum()    # 12.3\ncount = Query(data).filter(col &gt; 0).count()  # 5\nvmax  = Query(data).max()                    # 4.1\nvmin  = Query(data).filter(col &gt; 0).min()    # 0.7\n</code></pre>"},{"location":"examples/numeric/#supported-dsl-operators","title":"Supported DSL operators","text":"Expression Internal op SIMD <code>col &gt; x</code> FilterGt \u2705 <code>col &gt;= x</code> FilterGe \u2705 <code>col &lt; x</code> FilterLt \u2705 <code>col &lt;= x</code> FilterLe \u2705 <code>col.between(a, b)</code> FilterBetween \u2705 <code>col * x</code> MapMulScalar \u2705 <code>col + x</code> MapAddScalar \u2705 <code>col - x</code> MapSubScalar \u2705 <code>col / x</code> MapDivScalar \u2705 <code>-col</code> MapNeg \u2705 <code>col ** x</code> MapPowScalar \u2014 <code>col.abs()</code> MapAbs \u2014 <code>col.sqrt()</code> MapSqrt \u2014 <p>Chaining arithmetic</p> <p>Each <code>col</code> expression is one operation.  For multi-step transforms use multiple <code>.map()</code> calls: <pre><code># CORRECT\nQuery(data).map(col - 10).map(col / 90)\n</code></pre></p>"},{"location":"examples/numpy/","title":"NumPy Integration","text":"<p><code>from_numpy()</code> reads the array via the buffer protocol \u2014 one bulk memcpy, no per-element boxing.</p> <pre><code>import numpy as np\nfrom zpyflow import from_numpy, col\n\narr = np.random.standard_normal(1_000_000)\n\n# SIMD filter + map, GIL released, 1 allocation\nresult = (\n    from_numpy(arr)\n        .filter(col &gt; 0)\n        .map(col ** 2)\n        .take(10_000)\n        .to_list()\n)\n\n# Aggregations\npositive = from_numpy(arr).filter(col &gt; 0)\nmean_pos = positive.sum() / positive.count()\n</code></pre>"},{"location":"examples/numpy/#supported-dtypes","title":"Supported dtypes","text":"NumPy dtype ZPyFlow path <code>float64</code> f64 fast path (zero-copy numpy view) <code>int64</code> i64 fast path (zero-copy numpy view) <code>bool</code> u8 compact (compact storage, no boxing) <code>uint8</code> u8 compact <code>float32</code>, <code>float16</code> cast to float64 first <code>int32</code>, <code>int16</code>, <code>int8</code>, <code>uint16</code>, <code>uint32</code> cast to int64 first <code>uint64</code> Python fallback (<code>tolist()</code>) Other Python fallback (<code>list(arr)</code>)"}]}